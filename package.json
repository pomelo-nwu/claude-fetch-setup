{
  "name": "claude-fetch-setup",
  "version": "1.0.0",
  "description": "A tool to automatically setup fetch MCP for Claude Code in enterprise environments",
  "main": "index.js",
  "bin": {
    "claude-fetch-setup": "./index.js"
  },
  "scripts": {
    "lint": "echo lint",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "claude",
    "claude-code",
    "mcp",
    "fetch",
    "enterprise"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "fs-extra": "^11.1.1",
    "child_process": "^1.0.2"
  }
}
