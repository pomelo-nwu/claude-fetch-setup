{
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "name": "claude-fetch-setup",
  "version": "0.1.2",
  "description": "A tool to automatically setup fetch MCP and configure global CLAUDE.md for Claude Code in enterprise environments",
  "main": "index.js",
  "bin": {
    "claude-fetch-setup": "./index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pomelo-nwu/claude-fetch-setup.git"
  },
  "scripts": {
    "lint": "echo lint",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [
    "claude",
    "claude-code",
    "mcp",
    "fetch",
    "enterprise"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "fs-extra": "^11.1.1",
    "child_process": "^1.0.2"
  },
  "devDependencies": {
    "prettier": "^3.1.0"
  }
}
